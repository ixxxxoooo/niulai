# 工作规则（必须遵守）

每次修改代码后必须完成以下全部步骤：

1. **验证**：运行测试/构建/接口验证等方式确认修改正确无误。
2. **重启容器**：重新构建并启动 Docker 容器使新代码生效：
   ```
   docker compose up -d --build
   ```
   并验证服务可正常访问（`curl http://127.0.0.1:8088/` 返回 200）。
3. **Git 提交推送**：git add、commit（遵循仓库现有 commit 风格）、push 到 origin。
