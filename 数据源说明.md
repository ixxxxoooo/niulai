# 后端数据来源审计

> 统计日期：2026-08-15　范围：`backend/` 全部数据源代码 + 前端跳转链接
> 结论：**行情数据全部来自免费公开接口，无付费/账号依赖**；另有一处 AI 分析走用户自配的 DeepSeek API。

---

## 1. 东方财富（EastMoney）—— 主数据源，覆盖绝大多数行情

采用**多节点故障转移**：同一功能配置多个子域名，按优先级尝试，失败自动切换（见 `backend/config.py`）。

| 域名（节点） | 后端接口路径 | 对应业务 |
|---|---|---|
| `push2delay.eastmoney.com`<br>`1.push2delay.eastmoney.com`<br>`push2.eastmoney.com` | `/api/qt/ulist.np/get` | 指数快照、个股批量快照（自选）、板块行情 |
| 同上（榜单） | `/api/qt/clist/get` | 热门股榜 / 涨速榜 / 主力净流入榜 / ETF 榜 / 板块成分股 |
| 同上（个股） | `/api/qt/stock/get` | 个股实时快照（价格/涨跌/换手/量比/PE/PB/市值等） |
| 同上（明细） | `/api/qt/stock/details/get` | 个股成交明细（分笔） |
| `push2delay.eastmoney.com`<br>`1.push2delay.eastmoney.com`（资金流节点） | `/api/qt/stock/fflow/daykline/get`<br>`/api/qt/stock/fflow/kline/get` | 个股/板块/大盘资金流向（超大单/大单/中单/小单） |
| `push2his2.eastmoney.com`<br>`push2his.eastmoney.com`<br>`42.push2his` / `21.push2his` | `/api/qt/stock/kline/get` | 日/周/月 K 线（前复权） |
| 同上（历史节点） | `/api/qt/stock/trends2/get` | 分时走势 |
| `push2ex.eastmoney.com` | `getTopicZTPool` / `getTopicZBPool` | 涨停池 / 炸板池 |
| `searchapi.eastmoney.com` | `/api/suggest/get` | 股票模糊搜索（中文/拼音/代码） |
| `emweb.securities.eastmoney.com` | `/PC_HSF10/CoreConception/PageAjax` | 个股所属概念（F10 核心概念） |
| `datacenter-web.eastmoney.com` | `/api/data/v1/get` | 龙虎榜（每日席位明细） |
| `np-listapi.eastmoney.com` | `/comm/wap/getListInfo` | 个股资讯流（新闻列表） |
| `np-anotice-stock.eastmoney.com` | `/api/security/ann` | 个股公告 |
| `so.eastmoney.com` | `/news/soarweb/...` | 新闻搜索（降级途径） |
| `quote.eastmoney.com` / `data.eastmoney.com` | ——（仅前端跳转原链，非接口） | 详情页“东财↗”外部链接 |

---

## 2. 腾讯（Tencent）—— 个股详情 / 盘口 / K线备源

| 域名 | 接口 | 业务 |
|---|---|---|
| `qt.gtimg.cn` | `/q=` | 个股/指数实时快照 + 五档盘口（配合东财快照合并成个股详情） |
| `ifzq.gtimg.cn` | `/appstock/app/minute/query` | 分时数据（备源） |
| `ifzq.gtimg.cn` | `/appstock/app/fqkline/get` | 日/周/月 K 线（东财 K 线失败时降级，指数 K 线优先走腾讯） |

---

## 3. 同花顺（THS）—— 热榜

| 域名 | 接口 | 业务 |
|---|---|---|
| `dq.10jqka.com.cn` | `/fuyao/hot_list_data/out/hot_list/v1/stock` | A股热榜（小时榜 / 日榜，含热度与上榜分析文字） |

---

## 4. 新浪（Sina）—— 指数 K 线降级

| 域名 | 接口 | 业务 |
|---|---|---|
| `money.finance.sina.com.cn` | `CN_MarketData.getKLineData` | 指数日/周 K 线（腾讯也不可用时的备源） |

---

## 5. TickFlow —— K 线最终降级

- `tickflow` Python 免费库（`TickFlow.free()`），日/周/月 K 线，作为东财/腾讯都不可用时的最后兜底（限 60 次/分）。

---

## 6. 深度求索（DeepSeek）—— AI 分析（非行情数据）

- `https://api.deepseek.com`：通过后端 `/api/ai/chat` 转发，前端“AI 分析”面板使用。
- API Key、模型、Base URL 均在“设置”页自行配置，默认模型 `deepseek-chat` / `deepseek-reasoner`，也支持 OpenAI 兼容地址。

---

## 7. 前端内容抓取（代理）

- `/api/crawl-article`：后端代理抓取新闻/公告正文（跨域限制），由前端“查看全文”触发。

---

## 数据链路小结

```
指数/行情快照   东财(多节点故障转移) ──→ 腾讯 qt.gtimg.cn
个股详情        东财快照 + 腾讯盘口合并
K线             东财 push2his ──→ 腾讯 fqkline ──→ 新浪 ──→ TickFlow
分时            东财 trends2 ──→ 腾讯 minute/query
资金流          东财 fflow 节点（daykline / kline）
榜单/板块       东财 clist / ulist
热榜            同花顺
龙虎榜          东财 datacenter-web
新闻/公告       东财 np-listapi / np-anotice / so.eastmoney
AI 分析         DeepSeek（用户自配）
```
