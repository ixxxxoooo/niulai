services:
  niulai:
    build: .
    image: niulai:latest
    container_name: niulai
    ports:
      - "8088:8088"
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
