# 开发进度

## 项目当前状态（2026-08-16）

> 下文为倒序迭代日志，本段汇总最新状态。相关文档：`README.md`（使用/接口）、`PERFORMANCE.md`（性能与优化）、`数据源说明.md`（数据来源）、`PLAN.md`（需求设计）。

**定位**：自用 A 股盘中分析 Web 工具（FastAPI + Vue3 + SQLite，单端口 8088，数据全部来自免费公开行情接口）。

## 2026-08-18 修复两市量能误报缩量：改按成交额对比上一交易日

- 问题：`/api/market/volume` 原用「上证+深证成指日K 成交量(手)」对比前5日均，8/17 两市成交额放量（2.14→2.40万亿，+2459亿）却显示"缩量 0.95"。根因：8/17 涨的是高价科技股，同样金额买到的手数更少，成交量严重失真
- 修复：改用**成交额**判断。数据源腾讯 `newfqkline`（含历史成交额，万），上证指数+深证成指日K；对比**上一交易日**（业界惯例）
- 返回新增 `prev_amount`（上一交易日成交额）；前端提示文案改为「比上一交易日」
- 验证：8/17 返回 ratio 1.11 放量 +2446亿（与新闻报道一致）；`pytest` 32 通过；容器重启后接口正常

## 2026-08-18 涨跌家数补北交所（对齐通达信沪深京三市口径）

- 问题：涨跌家数原只统计沪深两市（上证指数+深证成指），通达信/同花顺为沪深京三市，导致少约 300 家
- 修复：`market_overview` 额外拉取北证50（0.899050）的涨跌家数（东财对该市场所有指数返回同一份全市场数据），合计入 up/down/flat
- 验证：8/17 现显示 4369涨/1095跌/153平（通达信 4335/1064/140，差异约 30~80 家为停牌股口径，可接受）；`pytest` 32 通过

---

## 2026-08-17 金属板块拆分：黄金/白银/铜/铝/锌/镍/锡/钴各独立

- 需求：黄金与白银分开，工业金属各金属单独展示，不再整合
- config metal 区从 2 个板块拆为 8 个：黄金（COMEX黄金+伦敦金）、白银（COMEX白银）、铜（COMEX铜+LME铜）、铝、锌、镍、锡（LME）、钴（港股洛阳钼业代表股，东财无钴期货）
- 后端 region=metal 返回 8 板块，接口/前端（metalBoards 遍历）无需改模板
- 验证：8 板块数据正常；`pytest` 32 通过；npm run build 通过；服务重启正常

---

## 2026-08-17 全球题材板块重构：美股17/日股6/韩股5 + 贵金属与工业金属

- 需求：按核心产业重组题材 —— 美股（AI算力/CPO/半导体存储/数据中心/云计算/商业航天/卫星/机器人/自动驾驶/电网/军工/新能源/锂电池/光伏/生物制药/消费/银行 17个）、日股（半导体设备/工业自动化/精密制造/汽车产业链/光学影像/消费电子娱乐 6个）、韩股（存储/半导体/电池/消费电子/互联网平台 5个）
- 新增金属板块（region=metal）：黄金白银（COMEX黄金/白银+伦敦金）与工业金属（LME综合铜铝锌镍锡+COMEX铜），直接用东财期货/现货行情，非股票代表股
- 后端 `GLOBAL_THEME_SECTORS` 重构，region 支持 metal；`GET /api/global/sectors?region=us|jp|kr|metal`
- 前端 Global.vue 新增「贵金属 / 工业金属」卡片（右上角截图，悬浮显示各金属涨跌）
- 验证：30 个板块单次合并请求 ~111ms；`pytest` 32 通过；npm run build 通过；服务重启后各 region 接口正常

---

## 2026-08-17 全球指数分区域分行展示（每排4个）

- 全球指数按区域分行：美股一排（纳斯达克/纳斯达克100/标普500/道琼斯，每排 4 个），日韩/港股一排（日经225/KOSPI/恒生）
- 后端 `IndexQuote` 新增 `region` 字段，`global_indices()` 填充区域标记（美股/日韩/亚太）
- 前端 Global.vue 按 region 分组渲染，覆盖 index-grid 为每排 4 列（窄屏自动降为 2 列）
- 验证：`pytest` 32 通过；npm run build 通过；服务重启后接口分组正确

---

## 2026-08-17 全球市场展示优化：指数卡片块 + 题材悬浮成分股

- 全球指数从表格改为卡片块展示（复用 index-grid/index-card 样式，名称/最新价/涨跌幅/涨跌，按涨跌着色，点击进详情）
- 题材板块默认只显示题材名 + 板块涨跌幅；鼠标悬浮题材块弹出成分股列表（名称 + 个股涨跌），避免常驻占用空间
- 验证：`pytest` 32 通过；npm run build 通过；服务重启后接口正常

---

## 2026-08-16 全球市场独立页面：指数+题材分区域，支持截图，性能优化

- 需求：全球市场从首页迁到一级导航「全球」；指数（美股/日韩/恒生）数值按涨跌着色；美股题材扩展至热门十余个，日股/韩股题材分开；各卡片右上角支持截图
- 后端：
  - `config.py` 用 `GLOBAL_THEME_SECTORS` 替换 `US_THEME_SECTORS`：美股 19 个题材（存储芯片/CPO/AI算力/半导体设备/AI软件/科技七巨头/芯片设计/云计算/网络安全/加密货币/核电/减肥药/生物医药/新能源车/医疗机器人/消费电子/流媒体/军工航天/银行）+ 日股 6 个 + 韩股 7 个，每板块按 region 分组（us/jp/kr）
  - `eastmoney.py` `us_sector_boards(region)` 支持区域过滤；性能优化：全部板块代表股一次性合并为**单个 ulist 请求**再分组计算（原来每板块一次请求）
  - `market.py` `GET /api/global/sectors?region=us|jp|kr`
- 前端：
  - 新增 `views/Global.vue` 全球市场页：全球指数卡片（含恒生，最新值按涨跌着色）+ 美股/日股/韩股三张题材卡片（主题块显示板块涨跌幅+成分股涨跌），每张卡片右上角截图按钮
  - `router.js` 加 `/global` 路由，`App.vue` 一级导航加「全球」
  - `Overview.vue` 移除全球区块（已迁至全球页）
  - `IndexDetail.vue` 未知 secid（如 `100.NDX`）时禁用 prev/next 导航防崩
- 验证：接口 `?region=us` 返回 19 题材、`jp` 6、`kr` 7；单次合并请求 132ms；`pytest` 32 通过；npm run build 通过

---

## 2026-08-16 全球市场：美股/日韩指数 + 题材板块涨跌幅

- 需求：盘面总览新增全球视角 —— 美股（标普500/纳斯达克/纳斯达克100/道琼斯）、日韩（日经225/KOSPI）指数，以及美股题材板块（存储芯片、CPO 等）整体涨跌幅
- 调研：东财无美股题材板块接口（仅科技/金融等大类），确定「代表股 → 板块涨跌幅」方案；实测确认东财 secid 前缀：105/106/107=美股、176=日股、177=韩股，批量 ulist 快照可混拉
- 后端：
  - `config.py` 新增 `US_THEME_SECTORS`：存储芯片/CPO·光模块/AI算力·GPU/半导体设备/晶圆代工/韩股存储/日股半导体，每板块配代表股 secid 清单
  - `eastmoney.py` 修正 `GLOBAL_INDICES` 名称（`100.NDX`=纳斯达克综指，`100.NDX100`=纳斯达克100，原误标）；新增 `global_stock_quotes()`（海外标的批量快照）与 `us_sector_boards()`（板块涨跌幅=代表股算术平均，含成分股快照）
  - `market.py` 新增 `GET /api/global/sectors`
- 前端：Overview.vue 盘面总览底部新增「全球指数」+「美股·题材板块」双卡片，独立 10s 慢轮询；指数卡片点击进入 IndexDetail（复用现有 secid 链路）
- 验证：接口返回正常（存储芯片 +4.71% 等）；`pytest tests/ -q` 32 通过；npm run build 通过

---

## 2026-08-16 品牌图标按压反馈

- 左上角牛来品牌图标点击跳首页时增加按压反馈（:active 缩放 0.88 + 半透明），并加 cursor:pointer
- 验证：npm run build 通过，服务加载最新构建 index-D_8_azTD.js

---

## 2026-08-16 飞书通知增加官方文档跳转链接与配置说明

- 设置页「飞书通知」新增「如何配置」行：跳转飞书开放平台官方文档（群聊添加自定义机器人），并补充配置步骤说明（飞书群→设置→群机器人→添加 Webhook 机器人→复制 Webhook）
- 验证：npm run build 通过，服务加载最新构建 index-7D13rP8f.js

---

## 2026-08-16 主题迁移设置页 + 新增跟随系统 + 设置页排版优化

- 顶栏移除主题切换按钮（设置页已有颜色主题），设置按钮去边框改无轮廓图标（icon-btn）
- 主题新增「跟随系统」：useSettings.js 新增 applyThemeMode/resolveThemeLight（dark/light/system），App.vue 应用并监听系统主题变化自动切换，Settings.vue 颜色主题三选项
- 设置页「通用」tab 排版优化：合并「主题与外观」+「列表过滤」为「外观与过滤」，「关于」并入两列 grid，卡片/行距/间距压缩（gap 14→10、行 padding 12→8、卡片 padding 收敛），消除碎片化空白
- 验证：npm run build 通过，服务加载最新构建 index-DB6McRhr.js

---

## 2026-08-16 移除板块强度榜子板块功能

- 反馈：子板块展示效果不符预期，去掉该功能及相关代码
- 后端：删除 _sub_sectors/_build_sub_sectors/_em_concepts/_EM_ALIAS/_match_em_sector 及东财概念缓存，sector_strengths 不再返回 sub_sectors
- 前端：SectorStrength.vue 删除子板块表、hasSub/toggleSub 及子板块样式，恢复纯列表
- 验证：接口字段仅剩 strength/change_pct/limit_up_count/main_inflow/top_stocks 等；npm run build 通过、服务已重启

---

## 2026-08-16 板块强度榜子板块重构：默认展开、与一级同构、仅高强度展示

- 反馈：子板块要默认展示（可收起）、有和一级一样的强度/涨跌幅/涨停等完整展示、且只在强度高时出现（不是概念 chips）
- 调研：开盘啦无子板块独立强度接口（多次探测 GetPlate_Info_QJ/GetPanKou/板块列表均无层级）→ 子板块涨跌幅/主力净额改用东财概念板块匹配（别名映射：光模块→光通信模块 等，实时真实数据）
- 后端：子板块从涨停股 concepts 聚合，每个子板块含 涨停数/涨跌幅(东财)/主力净额(东财)/封单额合计/领涨股
- 前端：板块行内嵌子板块小表（子板块/涨跌幅/涨停/封单额/主力净额/领涨股），默认展开、▾ 收起；仅 strength≥5000 且存在子板块时展示（算力/芯片/通信/机器人/医药）
- 验证：通信→光模块(2.93%/128亿/封单51.6亿)、CPO、MPO、光芯片；npm run build 通过、服务已重启

---

## 2026-08-16 板块强度榜支持子板块（可折叠展开）

- 需求：大板块（如通信）下应能展示子板块（如光模块），可收起/展开
- 调研：开盘啦接口无现成父子板块层级（GetPlateInfo_w38/GetYTFP_BKHX 均只有大板块）；改用涨停股 concepts 概念标签聚合出子板块
- 后端 sector_strengths 每项新增 `sub_sectors`（concepts 聚合去重计数，Top12）和完整 `stocks`（code/name/concepts）
- 前端 SectorStrength.vue：板块名旁「▾」展开子板块 chips（名称×计数），点击 chip 显示该子板块下涨停股（悬停分时、点击跳个股）
- 验证：通信→光模块×6/CPO×3/MPO×3/光芯片×2/覆铜板×2；npm run build 通过、服务已重启

---

## 2026-08-16 修正板块强度榜主力净额字段（GetPanKou pankou[5] 才是净额）

- 问题：sector_capital 误用 pankou[3]，实测 [3]+[4]=[5]，[3] 是主力买入、[4] 主力卖出、[5] 才是净额（源码注释有误）；导致展示主力买入金额（芯片 1968 亿）虚高
- 修复：main_inflow 改用 pankou[5]，并修正字段注释；与东财同板块量级一致（芯片 -7.2 亿、通信 166.7 亿、算力 58.5 亿）
- 验证：接口返回合理；npm run build 无需改动、服务已重启

---

## 2026-08-16 板块强度榜主力净额改为板块级真实数据（GetPanKou）

- 问题：原主力净额是涨停股 main_inflow 汇总（stock[13] 之和），与板块整体主力净额差异巨大（芯片 82.9 亿 vs 真实 1968.6 亿），会误导
- 修复：新增 `sector_capital()`（GetPanKou 板块盘口：成交额/涨跌幅/主力净额/涨跌家数），sector_strengths 并发取强度+资金；前端新增「涨跌幅」列
- 验证：算力 强度7686/涨跌幅3.178%/主力720亿，芯片 1968.6 亿（与板块级一致）；npm run build 通过、服务已重启

---

## 2026-08-16 板块强度榜领涨股支持悬停迷你分时图

- 领涨股 span 外包 MiniTrend（悬停分时小窗），点击仍跳个股；补齐 kpl-leader-host 布局
- 验证：npm run build 通过，服务加载最新构建 index-C7Dzz9sI.js

---

## 2026-08-16 板块强度榜增加右上角截图按钮

- SectorStrength.vue 卡片右上角加截图按钮（captureElement → 板块强度榜.png），强度说明移到按钮左侧
- 验证：npm run build 通过，服务加载最新构建 index-CdiU-33q.js

---

## 2026-08-16 板块强度榜（列表视图，按强度降序）

- 需求：板块强度用列表方式展示，类似开盘啦「板块行情」按强度排序
- 调研：kaipanla-crawler 无全量板块强度排行单接口（探测 GetPlate_Info_QJ/GetPanKou 空参均失败）→ 组合实现：涨停原因板块列表（GetPlateInfo_w38）+ 并发批量强度（GetPlate_Info_QJ）合成
- 后端新增 `sector_strengths()`：涨停相关板块按强度降序，含强度/涨停数/主力净额（汇总个股）/领涨股（带 code）；路由 `/kaipanla/sector-strengths`（ttl 30s，并发 max_workers=8）
- 前端：板块一级页新增第三个 tab「板块强度」（/sectors/strength，App.vue 板块下拉加入口）；新视图 SectorStrength.vue 列表展示（排名/板块/强度分级色/涨停数/主力净额/领涨股可点跳个股），30s 轮询，接口失败显示"暂不可用"
- 验证：8/14 数据 13 个涨停相关板块，算力 7686/芯片 7412/通信 7202 居前；curl 200、overview 无回归、npm run build 通过、服务已重启
- 说明：仅覆盖当日有涨停的板块（开盘啦无全量板块强度接口）；领涨股可点击跳个股

---

## 2026-08-16 接入开盘啦：涨停原因题材聚合 + 板块强度/分时

- 调研：开盘啦核心功能含涨停原因题材归因、市场情绪、竞价抢筹、异动、百日新高等；GitHub 上 `jinhao2003/kaipanla-crawler`（135★）抓开盘啦私有接口最全；Tushare `kpl_list` 官方但需 5000 积分且 T+1。选抓包接口（实时），接受合规风险并做容错降级
- 后端：新建 `datasource/kaipanla.py`（POST longhuvip.com、uuid 设备号、apiv=w42、低频+超时+异常全吞）+ 路由 `/kaipanla/*`（ttl 30s）+ config.KAIPANLA_ENABLED 一键开关（False 全返回空）。接口：涨停原因板块 GetPlateInfo_w38、板块强度 GetPlate_Info_QJ、板块分时 GetTrendIncremental、板块名→申万代码映射（每日缓存）
- 前端：
  - 连板梯队页新增「涨停原因」页签：按题材聚合涨停股（题材名+涨停数+个股连板/封板时间/封单额/主力资金/涨停原因/概念标签），点击跳个股，容错显示"暂不可用"
  - 板块详情页新增「板块强度 + 分时图」区块：按板块名匹配开盘啦申万代码（精确+双向包含），匹配不到或接口失败自动隐藏
- 验证：8/14 复盘数据 13 题材 63 涨停（reason/concepts/lbc 均正常）；板块强度（医药 5086）/分时（241 点）正常；KAIPANLA_ENABLED=False 全空验证通过；npm run build 通过；服务已重启
- 说明：涨停原因为复盘数据（盘中无当日则显示最近交易日）；板块强度/分时仅名称可匹配的板块显示（东财行业名≠开盘啦题材名时隐藏）；未做涨停池实时题材列（复盘数据与实时涨停池时间错配会误导）

---

## 2026-08-16 导航栏倒计时与休市并排，去掉「刷新」文字

- 倒计时去掉「刷新」前缀文字只留秒数，与休市/交易中 session 同一排（时钟时间之下），点击仍可手动刷新
- 验证：npm run build 通过，服务返回最新构建 index-dMdUq-8Z.js

---

## 2026-08-16 连板梯队截图按钮入卡片右上角 + 导航栏倒计时并入时钟区

- 连板梯队截图按钮从页面标题移到连板梯队卡片右上角；今日炸板卡片右上角新增独立截图按钮（ref zbCard + doScreenshotZb）
- 导航栏倒计时（刷新指示器）从搜索框左侧移入最右侧时钟块，去除原边框框样式改纯文本（圆点指示刷新中、点击仍可手动刷新），位于时间/session 之下
- 验证：npm run build 通过；服务返回最新构建 index-DNTeTAEJ.js

---

## 2026-08-16 连板梯队游资改「游」小徽章 + 悬浮弹窗徽章展示

- 反馈：卡片直接显示游资名显乱，且悬浮展示时也要用徽章样式
- 新增 `LadderYouzi.vue`：卡片上仅显示「游」小徽章（有游资即显示，含拉萨时红色），鼠标悬浮弹出 mini-pop 风格悬浮窗（260px），窗内用 youzi-badge 徽章逐个展示具体游资（拉萨红色警示），点击徽章跳转该游资动向页；「游」标放在个股名称后面
- 卡片不再直接显示游资名称，chip 宽度 150px→128px
- 验证：npm run build 通过；服务返回最新构建 index-CBbvfo-5.js

---

## 2026-08-16 盘面总览指数卡片加分时迷你图 + 精简次要信息

- 需求：指数卡片常驻分时缩略图直观看走势；隐藏成交/振幅/高低/涨跌平等次要信息（详情页 IndexDetail 可看）
- 后端新增 `GET /market/indices-trends`：@ttl_cache(30s) 并发拉取 5 个主要指数分时，只返回绘制字段 {time,price}，接口约 40KB/次
- 前端新增 `IndexSpark.vue`：纯 SVG 折线（昨收虚线基准、涨红跌绿、按涨跌幅比例缩放），无 echarts 实例开销；Overview 卡片删 index-sub 并插入迷你图，独立 30s 慢轮询（不随 5s load 高频拉）
- 性能：仅新增 1 个接口/30s，交易时段后端 30s 缓存 → 每 30s 一次东财批量；非交易时段自动 60s 缓存
- 验证：接口返回 5 指数各 242 点、二次命中缓存 4ms；npm run build 通过；服务重启后 overview/health/首页均正常

---

## 2026-08-16 连板梯队恢复直接显示游资徽章 + 加宽卡片 + 炸板标签并入名称

- 反馈：「游资 N」悬浮摘要不如直接展示，chip 太窄、炸板标签位置不合适
- 调整：游资徽章恢复逐个直接显示（名称/拉萨红标/点击跳游资页）；chip 最小宽度 96px→150px 解决太窄；炸板标签从底部 meta 移入个股名称右侧，meta 行只留首板时间（梯队）/涨跌幅（炸板区）
- 验证：npm run build 通过

---

## 2026-08-16 连板梯队游资徽章合并为悬浮摘要

- 痛点：连板梯队 chip 内多个游资徽章直接展示名称并堆叠换行，加游资后排版显乱
- 修复：Ladder.vue 游资徽章合并为单个「游资 N」摘要徽章（含拉萨天团等反向指标游资时整徽章红色警示），悬浮（data-tip）展示具体游资列表（含反向指标标注），点击跳转首个游资动向页；chip 内间距由 1px 调至 3px 提升层级清晰度；梯队与炸板两区块统一处理
- 验证：limit-up 接口 youzi 字段正常（17 只有游资，开开实业等含 3 游资）；npm run build 通过

---

## 2026-08-16 个股详情页游资徽章去掉跨席位去重

- 背景：用户给拉萨天团自定义配置「东方财富证券股份有限公司拉萨金融城南环路证券营业部」后，在个股详情页（如 301520）看不到命中——后端已识别（nick=拉萨天团），但前端按昵称全局去重只显示第一个拉萨席位徽章（东城区江苏大道），金融城南环路这行徽章被隐藏
- 修复：Stock.vue 删除 buildSeatDedup/_dupNick 去重逻辑，buy/sell 席位直接渲染，每个命中游资的席位行都显示昵称徽章（点击仍可跳转游资页）
- 验证：301520 买入席位东城区江苏大道、金融城南环路两行均显示「拉萨天团」徽章；npm run build 通过

---

## 2026-08-16 编辑游资区分内置/自定义席位，自定义可自由增删改

- 痛点：编辑弹窗把内置规则席位和自定义席位混在一个 textarea 里，用户分不清哪些能删改，怕误删内置不敢操作
- 后端 `update_seat_group` 重构：内置席位（source='builtin'）不再依赖提交内容，直接保留并统一同步属性；提交的 seats 视为自定义席位整体替换（可增可删可改），与内置同名时 IGNORE 避免重复
- 前端 Seats.vue：榜单席位列分组展示「自定义席位（橙/正常）」与「内置规则席位（灰/只读）」；来源列新增「内置+自定义」混合标记；编辑弹窗内置席位改为只读区块（自动保留、不可删改），自定义席位单独 textarea 自由编辑
- 命中：内置前缀、自定义席位、新增游资席位全部参与 classify_seat 模糊匹配（临时库 5 场景验证通过）
- 验证：只提交自定义席位不提交内置，保存后内置前缀仍在且属性同步；自定义席位改/删后旧名不再命中；新增游资可删可改

---

## 2026-08-16 编辑游资时保留内置前缀席位规则

- Bug：`update_seat_group` 编辑时 DELETE 全部席位后整体重写，内置前缀规则（如拉萨天团的「东方财富证券股份有限公司拉萨」）被删/改 source，导致其余具体营业部不再被识别为拉萨天团
- 修复：编辑时仅替换自定义席位，内置规则席位（source='builtin'）始终保留；提交的同名内置席位同步更新属性
- 验证：只提交南环路新席位、不带内置前缀，保存后内置前缀仍在，东城区/团结路等营业部继续正确识别

---

## 2026-08-16 游资配置页显示实际出现营业部

- 背景：拉萨天团等游资在 lhb_seats 只配置一条前缀席位（如「东方财富证券股份有限公司拉萨」），classify_seat 靠模糊匹配命中所有具体营业部，但 Seats 页只显示配置席位，看不出实际关联了哪些营业部
- 后端 `/lhb/seats` 增加 `real_seats`：从 lhb_records 按 nickname 聚合实际出现过的营业部（含最近上榜日）
- Seats.vue 游资列表席位列新增「实际出现」区块，展示真实营业部 + 最近日期

---

## 2026-08-16 个股龙虎榜席位游资标识去重

- 根源：`classify_seat` 返回 label=`游资·{nick}` 由 seat-badge 展示，nickname 又由 youzi-badge 单独展示，同一位席位昵称重复
- 修复：有 nickname 时 seat-badge 只显示「游资」类型，具体昵称仅由 youzi-badge 显示一次（跨买卖席位全局去重）

---

## 2026-08-16 个股龙虎榜游资去重 + 涨停池游资列自适应宽度

- 个股龙虎榜席位：同一游资（nickname）在买卖席位的多个营业部重复出现，现按新标准去重——整只股票只显示一次徽章（首个席位显示）
- 涨停池「游资」列：改为 flex-wrap 自动换行 + max-width，列宽随内容动态收缩，不再被多个徽章撑宽

---

## 2026-08-16 个股涨停标签去重 + 榜单行业可点击跳转

- 个股信号标签：命中连板（首板/N连板）时不再重复显示「涨停」标签（连板已含涨停语义）
- 涨速榜/主力净流入/热门榜（StockTable）：行业列加粗高亮，点击跳转对应行业板块
- 涨停池自定义表格：行业列同样加粗高亮可点击跳行业
- 行业跳转复用 `/api/sectors/concept-code?type=industry` 映射

---

## 2026-08-16 首板/连板徽章 + 涨停池共享缓存

- 新增 `GET /api/stocks/{code}/limit-tag`：个股连板/炸板标签接口
  - 命中涨停池返回 `{lbc, zb_count, kind:"zt", first_time}`；命中炸板池返回 `kind:"zb"`；都不命中返回 null
  - 复用涨停池/炸板池共享缓存，零额外东财请求
- 涨停池/炸板池接口统一共享缓存（固定拉 300 条，`limit` 仅截断展示）：
  - `common.py` 新增 `cached_limit_up_pool()` / `cached_limit_break_pool()`，带 ttl_cache
  - 修复原先 `/market/limit-up?limit=100` 与 `limit=300` 是不同缓存 key、会重复请求东财的问题
- 涨停池表格连板列徽章化：`1` 连板显示「首板」，`≥2` 显示「N连板」，点击跳转连板梯队 `/ladder`
- 个股页信号标签区新增「首板 / N连板」徽章（涨停中），或「炸板」徽章（炸板中），点击跳连板梯队；signal-tag 支持 `to` 字段点击跳转
- 连板梯队页原有炸板信号确认保留：楼层内 chip 带「炸N」标签 + 底部「今日炸板」区块

---

**已实现功能**
- 盘面总览（指数/成交额/涨跌家数/涨停数/量能）、板块排行与详情、板块异动、热门/资金/涨速榜、涨停池/炸板池/连板梯队、ETF 榜、同花顺热榜、全球指数
- 个股：搜索（代码/名称/拼音首字母/全拼）、快照、分时/K线（双源降级）、五档盘口、成交明细、资金流、龙虎榜（席位标签+上榜次数）、新闻公告、百度压力支撑、筹码分布、AI 分析（流式）
- 自选 / 持仓（盈亏/收益快照/流水）、价格/涨跌幅/涨速监控（桌面通知）、持仓异动监控、飞书通知、盘后选股（突破/金叉/放量）

**技术要点**
- 东财多节点故障转移 + 请求合并 + 节点冷却；非交易时段短超时 + 长缓存
- 数据源标签（东财/腾讯/东财+腾讯）；接口全部不可用时返回 503
- SQLite 存储自选/设置/持仓/监控/日志；WAL + 异步批量日志写入

**当前主要遗留问题**（详见交付说明）
1. 选股结果涨跌幅恒为空（`daily_bars` 未存 `change_pct`），突破规则在 `amount` 缺失（腾讯降级）时永不命中
2. 持仓异动监控的可选类型与请求类型不一致（竞价上涨/高开低开5日线永远收不到）
3. `/alerts/check` 无缓存，监控规则多时每 8s 产生 N 次外部行情请求
4. 全 A 日 K 同步为串行拉取，首次约 50~80 分钟
5. 龙虎榜个股查询阶段一仍最多 12 次串行逐日请求
6. `watchlist_remove` 会连带删除持仓（删除自选时持仓一并消失，需确认是否预期）

---

## 2026-08-16 持仓股异动监控

- 后端新增 `GET /api/alerts/check-changes`：持仓异动检测接口
  - 每次调用拉取全市场最新 200 条异动，与持仓股代码匹配
  - 支持按异动类型过滤（大笔买入/卖出、急速拉升/跳水、封涨停、打开涨停等）
  - 同一股票同类异动 120 秒冷却，避免重复通知
  - 命中后自动推送飞书卡片（红色告警，含个股名、异动类型、涨跌幅、时间）
- 前端 `useAlertNotify.js` 集成异动轮询：
  - 每 8 秒同步检查价格告警 + 持仓异动
  - 异动命中时弹出桌面通知（requireInteraction=true，点击跳转个股详情）
  - 独立去重集合，200 条上限自动淘汰
- 设置页新增「持仓异动监控」区块：
  - 开关：`changes_monitor_enabled`（默认开启）
  - 异动类型勾选：大笔买入/卖出、有大买盘/卖盘、竞价上涨、高开/低开5日线、急速拉升/跳水、封涨停/打开涨停
  - 配置持久化到 SQLite settings 表
- API `api.js` 新增 `alertsCheckChanges()`

## 2026-08-16 飞书通知 + 龙虎榜增强 + 盘后选股

### 飞书通知通道
- 新增 `backend/notify/feishu.py`：飞书自定义机器人 Webhook 推送交互卡片
  - `send_alert()`：监控告警推送（标的/触发条件/当前值/涨跌幅/跳转链接）
  - `send_screener_result()`：盘后选股结果推送（规则名/命中列表/个股链接）
  - `send_test()`：设置页测试推送验证 Webhook 配置
- `alerts.py` 接入飞书双通道：触发监控后同步推送浏览器通知 + 飞书卡片
- 设置页新增「飞书通知」区块：启用开关、Webhook URL、测试推送按钮
- API：`POST /api/notify/feishu/test`

### 龙虎榜席位标签化
- 新增 `backend/db/lhb_seats.py`：席位标签库
  - SQLite `lhb_seats` 表：nickname/tier/style/premium/seat_name
  - 内置约 25 位知名游资（章盟主/赵老哥/炒股养家/佛山无影脚/作手新一/拉萨天团等）
  - `classify_seat(name)` 分类函数：游资（殿堂/新生代/地方）/机构/北向/营业部
- `lhb.py` 增强：
  - `fetch_lhb_stock()` 返回结构升级：`appear_count`（近 60 日上榜次数）、`appear_dates`、`history`（近 10 次上榜摘要）
  - 每个席位行增加 `type/nickname/label/style/premium` 字段
- 前端 `Stock.vue` 龙虎榜区域增强：
  - 席位类型彩色徽章（殿堂金/新生代蓝/地方紫/机构绿/北向青/营业部灰）
  - 知名游资昵称展示（如「章盟主」「赵老哥」）
  - 近期上榜次数标签 + 历史上榜折叠列表
- 设置页新增「龙虎榜席位标签」区块：席位数量、重置同步按钮
- API：`GET /api/lhb/seats`、`POST /api/lhb/seats/sync`

### 盘后选股系统
- 新增 `backend/db/daily_sync.py`：全 A 日 K 增量同步引擎
  - `daily_bars` 表存储日 K 数据（code/trade_date/OHLCV）
  - 支持全量/增量同步，后台线程 + 进度回调 + 前端轮询
  - `start_daily_sync_job()` 启动后台同步
- 新增 `backend/analyzer/screener.py`：三规则选股引擎
  - 突破（breakout）：收盘 = 近 N 日最高且成交额达标
  - 金叉（golden_cross）：MA5 上穿 MA20
  - 放量（volume_surge）：今日量 > N 日均量 × 倍数
  - 结果入库：`screener_runs` + `screener_hits` 表
- 新增 `backend/api/routes/screener.py`：选股 API 路由
  - `POST /api/screener/sync-bars`：触发日 K 同步
  - `GET /api/screener/sync-status`：同步进度
  - `POST /api/screener/run`：执行选股
  - `GET /api/screener/rules`：规则列表
  - `GET /api/screener/runs`：历史任务
  - `GET /api/screener/runs/{id}`：任务详情
- 前端新增 `Screener.vue`（`#/screener`）选股页面：
  - 日 K 数据状态 + 同步按钮 + 进度条
  - 规则勾选 + 范围选择（全 A/自选）+ 飞书推送选项
  - 结果展示：Tab 切换规则、个股列表（代码/名称/收盘/涨跌幅/信号）
  - 历史任务列表 + 查看详情
- 导航栏新增「选股」入口

## 2026-08-16 项目命名统一 + 依赖安装 + Git 初始化

- 项目品牌名统一为「盯盘」（英文标识 `dingpan`）
  - `frontend/package.json` / `package-lock.json`：`a-stock-dashboard` → `dingpan`
  - `README.md` / `PLAN.md` / `start.sh`：标题与目录树由「A股盘面分析工具 / test/」改为「盯盘 / dingpan/」
  - UI、FastAPI `title`、页面 title 原本已是「盯盘」，保持不变
- 依赖：根目录重建 `.venv` 并安装 `requirements.txt`；`frontend` 执行 `npm install`（补齐缺失的 `node_modules`）
- 新增根目录 `.gitignore`（排除 `.venv`、`node_modules`、`data/`、`logs/` 等）
- 服务已启动：`http://127.0.0.1:8088`，`/api/health` 返回 ok
- Git：仓库此前未初始化，已 `git init` 并完成首次提交

## 2026-08-16 后端启动验证通过

- 现象：uvicorn 长时间不 LISTEN，像「起不来」；根因是项目在 `Documents` 下，冷启动从 `.venv` 读大量小文件极慢（本机可见 iCloud/CloudDocs 同步压力），非代码语法错误
- 处理：预热 `compileall` + 单进程启动；已在 `0.0.0.0:8088` 跑通
- 验证：`/api/health` ok；`/api/stocks/600519` 含 `data_source`；新闻 10 条、公告 8 条；搜索 `maotai` 命中且含 `pinyin_full`

## 2026-08-16 修复新闻/公告消失

- 原因：`routes` 拆分后 `stocks.py` 使用 `config.USER_AGENT` 但未导入 `config`，新闻/公告接口抛 `NameError`，前端 `.catch(() => [])` 吞掉后区块因空数据不渲染
- 修复：补上 `from ... import config`

## 2026-08-15 工程拆分 + 可选 UX + 文档/Mock 单测

- **Stock.vue 拆分**：`StockHeader` / `StockSnapshot` / `OrderBook` / `StockCharts`（`TrendChart`+`KlineChart`）/ `MoneyFlow` / `NewsAnnouncements`
- **routes 按域拆分**：`backend/api/routes/{market,stocks,watchlist,alerts,ai,meta}.py` + `common.py`
- **监控涨速**：`metric=zhangsu`（Alerts / AlertQuickModal / 后端校验）
- **拼音全拼**：`stocks.pinyin_full` + `pypinyin`；搜索支持 `maotai`
- **数据源标签**：个股详情 `data_source`/`fetched_at`；资金流空态可见来源
- **文档**：同步 README / PERFORMANCE；`tests/test_api.py` 改为 mock 外网

## 2026-08-15 AI 按钮还原 + 监控添加 UI + 拼音首字母模糊搜索

- AI「开始分析」按钮改回系统原配色（靛紫小圆角渐变，去掉粉紫胶囊/星星图标）
- 监控页「添加监控」改为纵向表单：搜索选股、指数快捷芯片、指标/条件分段按钮、阈值+备注、触发预览句
- 股票搜索：已有拼音首字母；增强中间模糊（如 `zmt`→贵州茅台）；顶栏/搜索框提示「代码/名称/拼音」；全拼（maotai）暂不支持

## 2026-08-15 截图无闪缩 + AI 按钮美化 + 板块资金截图 + 热榜解读说明

- 截图改为仅在 html2canvas 克隆文档加边框留白，不再改动真实 DOM，消除页面缩闪
- AI「开始分析」按钮改为胶囊渐变+图标样式
- 板块资金流向：净流入/净流出 TOP 图支持截图
- 同花顺热榜「解读」：上游只对部分标的提供 analyse；无正文时降级展示 analyse_title 或概念标签

## 2026-08-15 截图带卡片轮廓 + 连涨至少两天

- 截图默认外包留白并保留圆角边框，整块轮廓更清晰；AI 截图按钮固定在卡片右上角
- 连涨/连跌仅 ≥2 天显示（涨/跌 1 天不显示「连涨一天」）

## 2026-08-15 AI 思维链折叠 + 截图 + 连涨累计幅 + 分时浮窗

- AI：思维链（reasoning）默认收起可展开；修复分段解析/展示错乱；面板支持截图
- 连涨/连跌标签附带区间累计涨跌幅（如 `连涨3天 +8.52%`）
- 分时浮窗：涨跌幅单独成行（价格下一行「涨跌幅 ±x.xx%」），指数分时同步

## 2026-08-15 连涨天数展示 + AI 分析报错排查修复

- 个股页信号标签新增「连涨N天 / 连跌N天」（按日K收盘价连续涨跌，平盘中断）
- AI 排查结论：当前模型为 `deepseek-v4-flash`（推理型），会先推 `reasoning_content`；原前端只读 `delta.content`，思考阶段空白且 token 耗尽时误报「未返回内容」；HTTP 层虽 200 但流内错误未透传
- 修复：流式兼容 reasoning；`max_tokens` 提到 4000；超时 120s；上游错误/超时写入日志并回传前端；设置页补充 V4 Flash 选项与使用说明

## 2026-08-15 修复 BOLL 图例只隐藏上轨

- 日K 布林带上下轨原先系列名分别为 `BOLL` / `BOLL下`，图例只挂了前者，取消勾选时下轨仍显示
- 上下轨统一系列名为 `BOLL`，点一次图例同时显隐

## 2026-08-15 量能占位 / 公告正文 / 刷新点击 / 分时自适应 / 副图例去重

- 量能(较5日均量)：加载中只显示「—」，加载后显示「放量/缩量/平量 x.xx 倍」，去掉「日K加载中」换行
- 公告浮窗：东财详情页多为空壳，改为走 `np-cnotice-stock` 内容 API 拉取正文 + PDF 链接
- 导航栏倒计时：`S` → `s`，点击可手动触发当前页主轮询刷新
- 分时「普通/自适应」坐标：单边行情不再强制对称半屏空白，仍保留昨收 0% 线；设置项文案改为「自适应」
- 分时/日K：去掉图内与上方 Tab 重复的 MACD/KDJ/RSI 图例，均线/BOLL 图例保留

## 2026-08-15 新闻浮窗图片适配 + 个股列表内快速切换 + 去掉板块空白资金流

- 新闻/公告全文浮窗：`:deep` 约束图片/表格最大宽度；清洗 HTML 宽高与内联尺寸（前后端双保险），避免大图撑破浮窗
- 个股详情左右切换：按入口保存列表与返回页（`useStockNav` + `openStock(row, ctx)`）
  - 自选/持仓 → 同表内切换，返回「自选」
  - 板块成分 → 板块内切换，返回「板块」
  - 排行/梯队/盘面榜单等同理；切换用 `history.replace`，返回始终回入口页而非上一只个股
- 去掉板块详情「主力资金流」空白卡片（历史接口常不可用）

## 2026-08-15 个股/ETF 增加百度跳转 + 清除区间圈选残留

- 个股页标题旁东财链接旁增加「百度↗」：`finance.baidu.com/stock/ab-{code}?mainTab=概览`
- 东财链接修正：5/6/9 开头走沪市（含 51x ETF）
- 彻底移除 K 线 brush 配置残留；强制重建前端产物

## 2026-08-15 去掉区间统计 + 分时副图可关 + 禁用分时滚轮缩放

- 移除日K「区间」框选/浮层统计相关代码
- MACD/KDJ/RSI 再点一次可取消，只保留主图+成交量（默认仍为 MACD）
- 分时图禁用滚轮缩放/平移；日K 仍可缩放

## 2026-08-15 百度压力支撑 + K线悬浮窗优化

- 后端新增 `/api/stocks/{code}/baidu-sr`：代理百度 `get_analysis_quotation` 的 `yl`（压力）/`zc`（支撑），无需登录 cookie
- 个股页压力/支撑改为百度两条为主（默认勾选），旧本地多线算法仅作接口失败兜底
- K 线接口用百度 `newMarketData` 补齐成交额/涨跌额/涨跌幅（东财常断连降级腾讯时字段不全）
- 日/周/月 K 悬浮窗改为双列排版：时间/开收高低/涨跌额幅/量/额/换手

## 2026-08-15 分时/K线 Y 轴业界对齐与设置化

- 新增 `frontend/src/utils/chartScale.js`：分时三档（普通/满占/涨停板）+ K 线自适应/固定对称，默认 8% 边距
- 设置页「图表显示」增加：分时坐标、K线坐标；持久化 `trendYScale` / `klineYScale`，变更派发 `chart-scale-change` 即时重绘
- 个股/ETF（Stock.vue）、指数（IndexDetail.vue）、迷你分时（MiniTrend.vue）统一接入；指数无涨跌停时涨停板模式回退普通坐标
- K 线默认可见高低+均线/BOLL 极值自适应；修正指数 K 线原先「首收对称固定轴」问题；主图增加 inside dataZoom

## 2026-08-15 资金流总览精简为「当日主力净流入」单卡

- 去掉「全天合计净流入」「当日中小单净流入」两块，只保留当日主力净流入
- UI 优化为单卡横排：左侧标题+大号净流入（红涨绿跌），右侧徽标（净流入/净流出）+ 占比 + 主力流入/流出
- 移除 totalFlow / retailFlow 计算属性

## 2026-08-15 自选页微调：市值副行纯金额加粗 + 金额固定两位小数 + 成本步长 0.01

- 持仓表名称下方「当前市值」去掉「市值」文字，仅显示金额并加粗
- 持仓相关金额（市值/成本/盈亏/收益记录）一律固定保留两位小数（原大数只保留整数位）
- 成本价加减改为固定步长 0.01：56.01 −1 → 56.00 −1 → 55.99（原实现按末位细分一位，0.01→0.011，不符合预期，已修正）

## 2026-08-15 自选页重构：自选/持仓 Tab 分离 + 个股ETF分表 + 成本末位微调

- 页面改为两个 Tab：「自选」/「持仓」，不再同一页混排
  - 自选 Tab：仅行情（现价/涨跌幅/涨速/成交额/主力净流入），不含任何盈亏信息；持仓股带「持仓」小标记且仍在自选里（后端录入持仓会自动加自选）
  - 持仓 Tab：显示全部持仓（含不在自选的老数据，前端已把自选∪持仓合并拉行情），含盈亏；顶部整体持仓汇总（总市值/浮动盈亏/当日盈亏）
- 持仓内部分「个股持仓」「ETF 持仓」两张表，各自独立排序
- 持仓表列序调整：浮动盈亏、盈亏比 移到「现价」前；「仓位」移到靠后；「当前市值」移到个股名称下方副行
- 个股名称按盈亏着色：盈利红 / 亏损绿（沿用 up=红 down=绿）
- 录入/改仓弹窗：成本价改为「末位微调」步进（0.01 → +0.001 → 0.011；0.1 → +0.01；整数 → +0.1），数量保持 ±100（一手）
- 收益记录（按日快照）移到持仓 Tab 底部

## 2026-08-15 修复 588 科创板 ETF 被「隐藏科创板」过滤

- 现象：588170（科创半导体ETF华夏）加了自选但自选页个股/ETF表、持仓表都不显示
- 根因：`useListFilter.matchListFilter` 的「隐藏科创板」正则 `/^(688|689|588)/` 把 588 开头的科创板 ETF 也当科创板个股过滤；持仓表同样基于 `filtered`，故有持仓也会被隐藏
- 修复：科创板过滤仅匹配个股代码段 `688/689`，588 系列为 ETF 不再误伤

## 2026-08-15 资金流：修复首屏延迟 + 全天合计净流入/主力流入流出

- 修复资金流图首屏延迟：`loadSlow` 里 `flow.value = f` 后同步调用 `renderFlowChart`，但图表节点在 `v-if="flow.length"` 内，首帧 DOM 尚未渲染导致 `flowChartEl` 为空而直接 return，图表要等下一轮 10s 轮询才出现；改为 `nextTick` 后再渲染
- 切换个股时重置 `flow.value = []`，避免上一只股票的资金流数据残留
- 资金流总览新增「全天合计净流入」（超大单+大单+中单+小单净额之和），带 净流入/净流出 徽标与正负着色，直接回答当日整体是流入还是流出
- 主力卡片下方新增「流入 / 流出」：由东财 datacenter（RPT_DMSK_TS_STOCKNEW）的 SUPERDEAL/BIGDEAL_INFLOW/OUTFLOW 提供，已验证 主力流入-主力流出=当日主力净流入；中单/小单无总额（上游仅提供净额），故仅展示主力档流入流出
- 后端 `MoneyFlowDay` 新增 `main_in/main_out` 字段；`_moneyflow_datacenter` 解析总额，`_merge_moneyflow_gross` 按日期合并进 fflow 结果（datacenter 失败静默降级，不影响主链路）

## 2026-08-15 截图重构 + 自选页增强

- 新增统一截图工具 `useScreenshot.js`：`captureElement` 截图后优先复制图片到剪贴板，浏览器不支持/写入失败自动降级下载 PNG；截图时自动移除画面内截图按钮；toast 区分「已复制」/「已下载」
- 修复连板梯队截图「复制成文字」：原实现依赖 ClipboardItem，不支持时静默走下载但 toast 仍提示「已复制」，导致粘贴到的是剪贴板旧文字；现统一走新工具，保证拿到的是图片
- 个股/自选页三张表（我的持仓 / 个股 / ETF）各加截图按钮
- 持仓弹窗：新增默认 100 股（改仓保留原数量）、±100 快捷按钮、成本默认带当前价
- 我的持仓表列头可点击排序（现价/成本/持仓/仓位/市值/盈亏/盈亏比/当日涨跌/当日盈亏）
- 自选页金额（市值/成本/盈亏/收益记录）改为千分位展示，不再换算 万/亿

## 2026-08-15 搜索历史（localStorage）

- 搜索选中/回车后记录最近 10 条（code/name/type），去重，最新在前
- 输入框聚焦且为空时下拉展示「搜索历史」，支持点击、键盘上下选择、清空按钮

## 2026-08-15 修复搜索「+ 自选」误跳详情 / 添加后不显示

- `SearchSuggest` 的「+ 自选」按钮 mousedown 未阻止冒泡：点击会先触发父级建议项的 select（跳详情/触发 select），再触发 toggleWatch 把刚加的又取消掉，导致加了不显示
- 改为 `@mousedown.stop.prevent`，点「+ 自选」只切换自选、不再跳转
- 切换后发出 `toggle` 事件，自选页监听并立即重载列表

## 2026-08-15 自选页改版：个股/ETF 分表 + 恢复同花顺风格

- 顶部汇总只保留「总市值 + 当日盈亏」，去掉「个股持仓 / ETF持仓」卡片
- 自选拆成三张表：我的持仓（同花顺风格全列：现价/成本/持仓/仓位进度条/市值/浮动盈亏/盈亏比/当日涨跌幅/当日盈亏/操作）、个股自选、ETF 自选（两者始终分开）
- 持仓归「我的持仓」表，个股/ETF 表只放未持仓的自选；恢复以前的大字号表格样式
- 持仓数据逻辑不变（数量/成本/市值/盈亏计算照旧）

## 2026-08-15 实时时钟 + 截图 toast 提示

- 右上角时钟改为每秒实时刷新（原 15s 刷新，观感像不刷新页面就不走）；交易时段状态独立 15s 慢轮询
- 新增全局 toast（`useToast.js` + `ToastHost.vue`，右上角 flash 消息，带淡入/滑出动画）
- 个股 K 线 / 资金流 / 龙虎榜 / 连板梯队截图成功后显示「截图成功，已复制到剪贴板」，失败显示「截图失败」

## 2026-08-15 总览精简 / 指数分时修复 / 监控快捷弹窗 / 技术标记

- 总览页去掉「大盘资金流向」区块（接口仍保留，监控页等可复用）
- 修复指数分时均价：腾讯对指数误用个股均价公式导致均价线把 Y 轴压扁；指数改为量加权点位均价
- 指数详情分时图：昨收虚线、成交量副图、异常均价过滤、Y 轴含昨收
- 个股 / ETF / 指数详情增加「+ 监控」按钮，`AlertQuickModal` 弹窗快捷设置价格/点数/涨跌幅阈值
- 个股详情「昨收」后展示技术标记：金叉/死叉、多头/空头、突破/跌破 MA20、MACD 金叉死叉、放量/缩量、主力流入/流出、逼近涨停/跌停

## 2026-08-15 导航指数条 / 大盘资金流 / 价格监控桌面通知

- 导航栏常驻上证 / 创业板 / 科创50，跟随刷新间隔定时更新，点击进指数详情
- 接入东财大盘资金流（dpzjlx）：`/api/market/moneyflow`（总览已不再展示，可按需复用）
- 板块资金页补充东财原链：行业资金 / 净流入榜 / 个股资金
- 新增「监控」页：个股或指数可按价格、点数、涨跌幅设置 ≤/≥ 阈值；Chrome/macOS 桌面通知（Notification API）
- 接口：`/api/indices/quotes`、`/api/alerts` CRUD、`/api/alerts/check`

## 2026-08-15 UI优化: 总览布局/领涨股/板块排序/资金流数值

- 修复 `SectorMoves` 页面 `settingsState is not defined` 报错（缺少导入）
- 总览页去掉"Top 10"截断，板块/ETF/异动内容可上下滚动（scroll-list 420px）
- 盘中异动移至 ETF 上方
- 领涨股统一 `leader-chip` 样式：名称 + 红色涨幅，覆盖总览/板块列表/板块详情/资金流向
- 板块分析去掉右侧"按涨幅/按主力/按成交额"冗余按钮（列头已支持三态排序）
- 指数详情重构：核心数据（价格/涨跌/高低/成交）放到分时图上方，去掉底部重复"指数信息"卡片
- 个股资金流：柱状图上方增加明确数值行（红/绿色显示流入/流出）

## 2026-08-15 后端稳定性 / 徽标简化 / 资金流自适应

- **后端卡死修复**：
  - push2 主节点长期断连，改为优先使用 push2delay（延迟节点实测稳定）
  - 请求合并等待超时从 30s 降至 10s，超时后独立发请求，避免线程阻塞
  - 添加总请求截止时间（deadline = timeout × 2.5），单次最多阻塞 ~20s
  - 关闭 follow_redirects，重定向直接判为节点下线，快速失败
  - 资金流接口去掉 push2 主节点重试，仅走 delay 节点 + datacenter 兜底
- **徽标简化**：科→科创板、创→创业板、ST→ST、北→北交所（单字）
- **资金流标题自适应**：个股/板块资金流标题显示实际获取到的天数（非固定"近5日"）
  - 周末/盘后 delay 节点仅保留当日数据，属数据源限制

## 2026-08-15 标签展示 / 指数K线 / 持仓盈亏 / 炸板

- 概念/行业写入 SQLite，列表与详情展示；同步改为后台任务 + 进度条
- 详情页用本地标签秒出名称，避免先闪代码
- 列头三态排序（降序 → 升序 → 取消）；去掉热门/ETF 第二排排序按钮
- 科创/创业/ST/北交徽标覆盖列表、详情、连板、搜索
- 指数 K 线改走腾讯（已验证上证/深成/创业板日周月可用），新浪日/周兜底
- 连板时间 zfill 修复；个股带行业；补充炸板池
- 自选持仓录入 + 顶部个股/ETF 浮动盈亏与按日快照
- 5 日资金流改走东财 delay 节点（同花顺资金页 401 不可用）

## 2026-08-15 连板楼层 / 热榜龙虎榜 / 标签过滤 / K线指标

- 连板梯队独立一级 Tab，楼层排列（最高连板在上，自动换行无滚动条）
- 同花顺热榜 + 东财龙虎榜；个股详情展示最近上榜席位
- 自选拆个股/ETF；batch 用 ulist 补齐涨速、主力净流入
- 设置：隐藏科创/创业/ST/北交；名称/行业/概念手动或定时同步到 SQLite
- K 线/分时附图 MACD、KDJ、RSI + BOLL；指数改用 secid 查询，修复 A 股指数 K 线
- 名称悬停分时小窗；首页 ETF / 涨停跳转对应榜单

## 2026-08-15 验证修复：分时双轴/压力可选/标签前置/连板还原

- 分时图：上下限=涨跌停价；去掉带「0%」文字的手动零轴；中间网格对应昨收，略加深显示
- 压力支撑：去掉整数关口/昨收；近期高点不重复；改为勾选单条显示，图上不写字（防密集）
- 个股标签：全站移到名称前面（详情/总览/板块/自选/连板/排行）
- 连板梯队：恢复原楼层标题样式 + SVG 截图
- 主力流入：用资金流当日金额校正快照 f62 异常值
- 新闻：修复东财搜索返回结构解析（list 直出）
- AI：确认走 `/api/ai/chat` 后端代理，前端不直连第三方
- 资金流历史：上游东财当前仅返回 1 日（已实测），UI 提示「数据源暂仅返回 N 日」

## 2026-08-15 AI后端代理 + 分时图双轴 + 连板梯队UI重构

- AI 分析改为后端代理接口（`/api/ai/chat`），避免前端暴露 API Key 和 CORS 问题
- 分时图双 Y 轴：左轴价格、右轴百分比，范围按涨跌幅限制（主板±10%、创业板/科创±20%、ST±5%）
- 昨收零轴线标记"0%"，不再显示具体金额
- 压力支撑位：移除"昨收"和"整数关口"，只保留 MA/近期高低/BOLL
- 主力流入标签金额修正（修复负数格式化 double-negative bug）
- 资金流后端 lmt 参数提升至 30，增加数据获取成功率
- 截图按钮改为 SVG 图标（非 emoji），扩展到资金流图表 + 龙虎榜 + 连板梯队
- 连板梯队样式重构：楼层左侧放"几连板"数字，右侧排列 chip，更美观
- 总览页面移除"数据时间"和"盘中状态"
- 所有标签（科创/ST/创业板）移到个股名称前面显示
- 悬浮浮窗增加涨跌幅（带颜色）+ 行业标签
- 新闻接口改进：使用股票名称搜索 + 降级东财资讯流

## 2026-08-15 AI流式分析 + 图表交互优化 + 多项UI修复

- AI分析面板：修复点击不生效（hasConfig检查优化）、实现流式输出、富文本渲染（数值/看多/看空关键词自动着色）
- 压力支撑线：日K仅显示最近2条支撑+2条压力，避免密集遮挡
- 主力流入标签：修复消失问题（阈值从1万降为非零）、金额直接显示
- 领涨股标签：Overview/Sectors/SectorDetail/SectorMoves 统一添加科创/ST/创业板/北交标签
- 底部指数条：去掉胶囊背景色，hover才显示
- 分时图：昨收零轴线默认显示、tooltip增加涨跌幅%（带颜色）+成交额、买卖点默认展示
- 分时图截图：一键复制到剪贴板（降级为下载）
- 日K金叉死叉：仅在日K副图标注、分时图不标注
- MACD长度修正：日K请求120天数据（原60天导致MACD前35点为null）
- 新闻弹窗：点击新闻项弹窗展示（不跳转外部）
- 主力资金流：支持5/10/20日快捷切换，数据来源支持最多30日历史

## 2026-08-15 SQLite 存储 + 性能优化 + 连板梯队 + 日志

- SQLite（`data/stock.db`）：stocks / watchlist / settings / api_logs / user_actions / ds_logs
- 启动后台同步全 A + ETF 名称（含拼音首字母），搜索走本地，未命中再调东财
- 自选、设置改存 SQLite；首次打开迁移当前 origin 的 localStorage
- 东财节点健康冷却、请求合并、非交易时段短超时 + 60s 缓存
- 板块详情改为单板块快照 + 成分股并发，不再扫全部板块列表
- 热门页增加连板梯队视图（数据仍用涨停池 lbc）
- 设置页可查看接口耗时 / 数据源 / 页面操作日志

## 2026-08-16 游资席位入库管理（CRUD + Settings 管理表格）

- lhb_seats 表新增 source 字段（builtin/custom），旧库自动 ALTER 迁移（默认 builtin）
- 代码 _BUILTIN_SEATS 改名 DEFAULT_SEATS 种子，仅用于首次建库/恢复出厂；运行期数据以 SQLite 为唯一权威
- init_builtin_seats(force=True) 语义改为：仅重建 builtin 条目、保留 custom（原为清空整表）
- 新增席位 CRUD：POST /api/lhb/seats、PUT/DELETE /api/lhb/seats/{nickname}，写库后自动 reload 内存缓存（classify 立即生效，无需重启）
- Settings 页「龙虎榜席位标签」改造为「游资榜单」管理表格：按昵称分组展示（级别/属性中文映射、来源标记）、新增/编辑弹窗（多席位）、删除确认、「恢复内置字典」保留自定义
- 端到端验证通过：新增→count+1、重复新增 409、更新→source 变 custom、删除恢复、恢复内置保留 custom（98 builtin + 1 custom）

## 2026-08-16 设置页排版优化（分组导航）

- 顶部新增吸顶分组导航条（sticky），设置项归为 4 组：通用（主题/刷新/图表/过滤/关于）、数据（自选股/数据同步/游资榜单）、AI 与通知（AI 分析/飞书/异动监控）、日志（运行日志）
- 通用组短卡片改为两列网格并排（settings-grid，窄屏自动单列），减少纵向滚动
- 「关于」作为通用组页脚卡片；游资编辑弹窗置于分组外层（fixed 不受切换影响）
- 分组切换用 v-if（数据已在内存 ref，切换无额外请求）

## 2026-08-16 游资独立页面 + 游资动向（买入记录追踪）

- 新增「游资」页面（/seats，导航栏新增）：单页双 tab —— 游资榜单 + 游资动向；游资榜单从设置页整体迁出，新增筛选（级别/属性/昵称搜索）
- 新增 lhb_records 表（全量席位明细：date/code/side/seat_name/type/nickname/buy/sell/net/reason）+ lhb_dates 表（已同步交易日）
- 新增 backend/db/lhb_moves.py：按日期拉东财全市场买卖席位（RPT_BILLBOARD_DAILYDETAILSBUY/SELL 分页拉全）→ classify 识别 → 入库（增量去重，已同步日期跳过）；过滤散户统计席位（自然人/其他自然人/中小投资者/机构）
- 新增 API：GET /lhb/moves/dates、GET /lhb/moves?date=&side=、GET /lhb/moves/{nickname}、POST /lhb/moves/sync（后台任务+进度轮询）、POST/GET /lhb/moves/auto（每日自动同步开关）
- 每日收盘后自动同步当天龙虎榜（sync.py 后台循环，settings lhbAutoSync 默认开启）
- 前端 Seats.vue 游资动向：日期选择（快捷日期、无数据置灰）、按日期查看买入/卖出（股票聚合+参与游资）、点击游资看其买入汇总（哪只票哪天买入、次数、累计额、逐次明细）；股票可跳转个股页
- 已同步 2026-08-10 ~ 08-14 共 2825 条席位（游资相关 473 条）；验证通过（孙哥 42 只票、美诺华 8/10-8/14 买入 4 次）

## 2026-08-16 补充：游资动向筛选 + 数据一致性修复

- 游资动向新增「按游资筛选」：下拉选择游资（或点击当日列表游资 chip）→ 主表格直接展示该游资买入的股票汇总（首次/最近买入、次数、累计买入、逐次明细）；移除原弹窗
- 修复 moves_by_date 数据缺失 bug：SQL 原对席位明细 LIMIT 100（按净额），导致部分股票的游资席位被截断、聚合显示为空；改为全量取出再聚合、仅对股票数量限制 → 与个股详情页席位数据一致
- 已全量对比验证：8/14 共 67 只上榜股，26 只当天确无游资席位（仅机构/北向/普通营业部），个股详情页结果一致
- 两边逻辑确认同源：均用东财 RPT_BILLBOARD_DAILYDETAILSBUY/SELL + 同一 classify_seat；唯一差异为详情页单股最多展示前 10 条席位（页面限制）、动向存全量席位

## 2026-08-16 AI 智能分析改为浮窗 + 历史记录 + Markdown 预览

- AI 分析从内嵌卡片改为浮窗弹窗；入口为个股页顶栏新增的扁平「✨ AI」按钮（StockHeader，emit open-ai）
- 弹窗内思考过程与正文均用 marked 渲染 Markdown（实时预览：标题/列表/表格/代码块/引用），已清洗 script/iframe/事件属性
- 新增 ai_history 表 + API：POST /ai/save（保存后仅保留该股最新 5 条）、GET /ai/history/{code}（倒序）
- 历史机制：展开默认展示最近一条（当天最新优先）；顶栏下拉可切换查看 5 条历史；仅点击「重新生成」才触发新分析并更新记录
- 安装依赖 marked@18

## 2026-08-16 个股页 AI 浮窗打磨 + 概念标签跳转板块

- AI 浮窗：新增截图按钮（html2canvas 截取整个浮窗含标题栏边缘）；截图按钮置于关闭按钮左侧
- 打开「AI分析」：当天无记录则自动开始分析，有记录则直接展示上一次结果（不自动跑）
- AI 结果恢复关键词着色（参考旧版 renderRichText）：百分比/accent、价格/橙、看多买入/红、看空卖出/绿、震荡观望/灰、止损风险/橙黄；实现为先占位 HTML 标签、仅对纯文本着色再还原，不污染 Markdown 标签
- 去掉个股页顶栏「数据来源（东财/腾讯）」标签与「行情时间」
- 个股快照「板块概念」标签可点击：新增 GET /api/sectors/concept-code?name=xx，按名称映射东财概念板块代码（BKxxxx，全量列表 5 分钟缓存，支持子串双向模糊匹配），命中后跳转 /sector/{code} 板块详情页
- 「板块概念」标签点击跳板块页：新增 GET /api/sectors/concept-code 支持 concept/industry/area 三型回退。东财个股板块标签会混入行业/地区板块名（如"影视院线"实为行业板块、"北京板块"实为地区板块），先按 type 查，查不到自动回退另两类；板块全量列表按类型缓存 5 分钟。行业标签同样支持点击跳转
