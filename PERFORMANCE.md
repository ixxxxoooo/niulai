# 性能与数据源分析

本文档梳理各接口/页面功能的**数据组装方式、外部请求次数、典型耗时**，以及免费接口的优化空间。
用于排查"哪些地方慢 / 哪些功能是拼装出来的、比较耗时"。

> 实测环境：2026-08-14 收盘后，本机网络直连（东财 push2delay 节点可用、push2his 历史节点被风控）。
> 单位：一次 HTTP 请求约 100~500ms（受免费接口响应速度影响）。
> 文档更新：2026-08-16（对齐新增接口：龙虎榜/异动/筹码/选股/监控轮询）。

---

## 一、各接口的数据组装与耗时

| 接口 | 组装的外部请求 | 请求数 | 典型耗时 | 说明 |
|---|---|---|---|---|
| `/api/market/overview` 大盘概况 | ①指数 ulist ②涨停池 ③上证指数日K(量能) ④腾讯盘口(时间戳) | 3~4 | 0.5~2s | **聚合最多**：4 个不同数据源串/并行。涨停池、日K 依赖易被风控的节点，降级后变慢 |
| `/api/sectors` 板块排行 | ①东财 clist | 1 | 0.2~0.5s | 轻量，推荐 |
| `/api/sectors/{code}` 板块详情 | ①板块列表(找元信息,最多2类) ②成分股 clist | 2~3 | 0.3~1s | 板块元信息要遍历行业+概念列表各 500 条定位，偏重 |
| `/api/sector-moves` 板块异动 | **并发分页拉全部板块**（行业+概念约 500~700 条，每页 100 → 6~8 页并发） | 6~8(并发) | ~1s | **组装较重**：东财 clist 单页上限 100 且 `po=-1`(升序) 不支持，只能全量拉取后本地排序 |
| `/api/rank/*` 榜单 | ①clist 单页 | 1 | 0.2~0.5s | 轻量 |
| `/api/stocks/{code}` 个股详情 | ①东财个股快照 ②腾讯盘口(GBK解析) | 1~2 | 0.3~0.8s | 双源合并，东财挂时腾讯单独构造；响应含 `data_source` |
| `/api/stocks/{code}/trends` 分时 | ①东财 push2his trends2 →(失败)②腾讯 minute | 1~2 | 0.3~0.8s | **东财历史节点当前被风控，实际走腾讯**，腾讯还能推导均价线 |
| `/api/stocks/{code}/kline` K线 | ①东财 kline →(失败)②腾讯 fqkline | 1~2 | 0.3~0.8s | 同上，走腾讯；可用百度补字段 |
| `/api/stocks/{code}/ticks` 成交明细 | ①东财 details | 1 | 0.2~0.5s | 轻量 |
| `/api/stocks/{code}/moneyflow` 个股资金流历史 | ①东财 fflow kline | 1 | 0.2~0.8s | 节点被风控时返回空（已容错） |
| `/api/sectors/{code}/moneyflow-history` 板块资金流历史 | ①东财 fflow daykline | 1 | 0.2~0.8s | 节点被风控时 `available:false`（已容错） |
| `/api/global/indices` 全球指数 | ①东财 ulist(6个海外指数) | 1 | 0.2~0.5s | 轻量 |
| `/api/global/{secid}/trends` 全球分时 | ①东财 trends2 →(失败)②腾讯(恒指/美股可用) | 1~2 | 0.3~0.8s | 日经/韩综腾讯不支持 → 东财恢复前显示"暂不可用" |
| `/api/global/{secid}/kline` 全球K线 | ①东财 kline →(失败)②腾讯 | 1~2 | 0.3~0.8s | 同上 |
| `/api/search` 股票搜索 | ①本地 SQLite（首字母+全拼）②未命中东财 suggest | 0~1 | <0.1s / 0.2~0.4s | 前端 200ms 防抖 |
| `/api/stocks/{code}/lhb` 个股龙虎榜 | ①逐日找最新上榜日(最多12次串行) ②买/卖席位各1 ③近90天批量1 | 3~14 | 0.5~2s | **阶段1最多 12 次串行请求**，可再优化 |
| `/api/market/lhb` 龙虎榜列表 | ①逐日找最近交易日 | 1~10 | 0.3~1.5s | 有数据即停 |
| `/api/market/stock-changes` 盘中异动 | ①push2ex 单次 | 1 | 0.2~0.5s | 15s 缓存 |
| `/api/stocks/{code}/chip` 筹码分布 | ①东财日K(复用降级链) | 1~2 | 0.3~0.8s | 60s 缓存，本地模型估算 |
| `/api/stocks/{code}/analysis-data` AI聚合 | **并发5路**：快照+分时+K线+资金流+新闻 | 5(并发) | 0.6~1.2s | 供 AI 分析，5s 缓存 |
| `/api/alerts/check` 监控检查 | **N × 个股/指数行情**（无缓存） | N | N×0.3~0.8s | N=启用监控数；**随监控增多线性变重** |
| `/api/alerts/check-changes` 持仓异动 | ①push2ex 全市场异动 | 1 | 0.2~0.5s | **每8s直接拉，无缓存** |
| `/api/screener/sync-bars` 日K同步 | ①每只 kline（串行） | 总股票数 | 首次约50~80min | **串行拉取为主瓶颈** |
| `/api/screener/run` 选股 | ①本地 daily_bars 全量扫描 | 0 | ~2~10s | 单线程逐只查库 |
| ~~`/api/market/distribution` 涨跌分布~~ | **并发 56 页拉全市场 5549 只** | 56(并发) | ~1s | **已移除** |

## 二、前端轮询负载（当前配置）

| 页面 | 频率 | 每次请求数 | 说明 |
|---|---|---|---|
| 盘面总览 | 5s | 5（overview+sectors×2+涨速+全球） | overview 本身聚合 4 个数据源 |
| 板块分析 | 5s | 1 | 轻 |
| 板块异动 | **5s** | 1（内部并发 6~8 页） | **页面上最重的轮询**（已从 3s 调到 5s） |
| 榜单 | 3s | 1 | 轻 |
| 个股页 | 3s + 10s | 2 + 若干 | 快照/分时 3s；明细/资金流/日K 10s |
| 自选/持仓 | 3s | 1（批量） | ulist 批量拉涨速/主力净流入 |
| 监控检查 | 后台轮询 8s | 1 | `/api/alerts/check`（内部 N 次行情） + `/api/alerts/check-changes` |
| 盘后选股 | 手动 | 0 | 日K同步后本地扫描，不轮询 |

**峰值估算**：盘面总览单次刷新 ≈ 4+1+1+1+1 = 8~10 次外部请求（含 overview 内部聚合）。免费接口对高频请求会临时风控。

## 三、"组装出来的"功能清单（拼装程度由高到低）

1. **大盘概况** — 4 个数据源拼装：指数 + 涨停池 + 上证量能(需K线) + 时间戳(腾讯)。
2. **板块异动** — 全量板块并发分页拉取后本地排序（东财不支持升序）。
3. **个股详情** — 东财快照 + 腾讯盘口双源合并；东财挂时腾讯单独构造（盘口/内外盘/委差/均价全来自腾讯）。
4. **个股分时/K线** — 东财优先、腾讯降级的双源链路；腾讯 K 线无成交额、分时需自行推导均价线。
5. **全球指数详情** — 东财 ulist + 腾讯分时/K线（部分指数腾讯无数据）。
6. **个股龙虎榜** — 逐日定位上榜日 + 买卖席位明细 + 近 90 天批量历史，多阶段串行。
7. **筹码分布** — 基于日 K 用换手衰减 + 均匀分布模型本地估算。
8. **AI 分析聚合** — 并发 5 路数据源（快照/分时/K线/资金流/新闻）+ 百度压力支撑。
9. **持仓异动监控** — push2ex 全市场异动 + 本地持仓匹配 + 冷却去重 + 飞书推送。

## 四、优化项状态（按收益）

| # | 建议 | 状态 |
|---|---|---|
| 1 | 降频「板块异动」轮询 3s→5~10s；或后端加长缓存 | **已落地前端 5s**；后端独立更长 TTL（8~15s）仍可继续 |
| 2 | overview 拆细粒度缓存：涨停池 TTL 10~15s | **未落地** |
| 3 | 板块详情元信息映射缓存 | **已落地**（改单板块快照 `sector_quote`，不再扫全量板块） |
| 4 | push2his 连续失败 N 次后本会话直降腾讯 | **已落地**（节点冷却 + 多节点故障转移；指数优先腾讯） |
| 5 | 总览合并 `/api/market/dashboard` | **未落地**（非必须） |
| 6 | 非交易时段前端降频 + 后端长缓存 | **已落地** |
| 7 | K 线前端 60s 缓存 | **已落地**（`useKlineCache`） |
| 8 | 拆分 `Stock.vue` / `routes.py` | **已落地**（2026-08-15） |
| 9 | 关键 API mock 单测、拼音全拼、监控涨速、数据源标签 | **已落地**（2026-08-15） |
| 10 | 龙虎榜 60 天逐日查询阻塞全部请求 | **已落地**（两阶段：逐日找最新日 + 近90天批量） |
| 11 | 日K/分时双源降级 + 百度补 K 线字段 | **已落地** |
| 12 | `/api/alerts/check` 加缓存或批量拉行情（监控多时避免每8s N 次请求） | **未落地** |
| 13 | `/api/alerts/check-changes` 复用 `/market/stock-changes` 的 15s 缓存 | **未落地** |
| 14 | 全 A 日 K 同步并发拉取（现串行，首次约 50~80 分钟） | **未落地** |
| 15 | 选股扫描按规则分线程 / 一次取全量 bars 内存扫描 | **未落地** |

## 四之二、新功能开销说明（2026-08-16 新增）

- **持仓异动监控**：后端每 8 秒直连 push2ex 拉全市场 200 条异动再本地匹配持仓（`alerts/check-changes`）。与总览/异动榜的 `stock-changes` 是两套独立请求，建议共享一层 15s 缓存。
- **盘后选股**：首次全 A 日 K 同步约 50~80 分钟（单线程串行 kline），增量只补最新交易日，很快。选股扫描是纯本地 SQLite 查询。
- **龙虎榜席位**：`lhb_seats` 全量载入内存做模糊匹配，单次分类 O(席位数)，开销可忽略。

## 五、免费接口的固有约束（为什么有些功能"看起来没数据"）

- **push2his（分时/K线/资金流历史）**：本会话多次被临时风控（返回 HTML 反爬页或拒连），恢复时间不定 → 工具已做腾讯降级 + 空数据容错 + 数据源标签。
- **北向资金**：交易所已停止盘中披露，公开接口无实时值（未实现）。
- **跌停池**：东财对应接口 404，情绪指标仅用涨停家数。
- **日经/韩综分时K线**：腾讯不支持，仅东财可用（被风控时显示暂不可用）。
- **同花顺热榜全文解读**：上游只对部分票给 `analyse`。
- **腾讯 K 线缺成交额/涨跌额**：东财断连降级腾讯时 K 线字段不全 → 用百度 `newMarketData` 补齐，仍失败则本地估算（量×价）。
- **全 A 日 K 同步受免费接口限速**：首次较慢；`amount` 缺失会导致突破规则漏命中。
