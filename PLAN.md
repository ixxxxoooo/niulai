# 牛来 niulai— 需求说明书与实施计划

## 一、项目定位

一个**自用的 A 股盘中分析 Web 工具**：打开浏览器即可一屏看到大盘概况、板块轮动、热门股与资金流向，盘中 3~5 秒自动刷新，支持随时查询任意个股的实时行情。

- 目标市场：A 股沪深两市（含科创板/创业板指数）
- 数据来源：免费公开行情接口（东方财富行情接口为主，akshare 为备选对照）
- 形态：Web 网页应用（浏览器访问，自动轮询刷新）
- 技术栈：Python 后端 + 轻量前端（Vue3 + ECharts 或极简静态页）

> ⚠️ 合规声明：仅作个人学习与行情分析，不构成投资建议；数据版权归各数据源所有，请勿用于商业分发。

---

## 二、功能描述（需求 → 功能）

你的三条原始需求，翻译为可开发的功能定义如下：

### 功能一：盘面与板块分析

**目标**：一屏看清"今天市场整体怎么样、钱在往哪些板块走"。

| 模块 | 核心内容 | 关键指标 |
|---|---|---|
| 大盘盘面 | 主要指数实时行情 | 上证指数、深证成指、创业板指、科创50 的现价、涨跌幅、成交额 |
| 市场情绪 | 市场温度计 | 两市总成交额、上涨/下跌/平盘家数、涨停/跌停家数 |
| 板块排行 | 板块涨跌全景 | 行业板块、概念板块按涨跌幅排序的列表（含领涨股、板块内涨跌家数） |
| 板块详情 | 板块内部分析 | 点击板块 → 成分股列表、板块资金流、板块涨跌分布 |

**交互**：盘面总览页 + 板块列表页 + 板块详情页；板块数据随轮询自动刷新。

### 功能二：热门股与资金流向

**目标**：找到"资金正在往哪里走"，捕捉盘中异动与拉升。

| 模块 | 核心内容 | 关键指标 |
|---|---|---|
| 热门股榜单 | 多维度排名 | 按涨幅、成交额、换手率、量比排名的个股榜单 |
| 个股资金流 | 主力资金动向 | 主力净流入、超大单/大单/中单/小单拆分、净流入占成交额比 |
| 涨速榜 | 盘中异动捕捉 | 涨速 = 现价相对 5 分钟前的涨幅；涨速榜实时更新，捕捉快速拉升 |
| 板块资金流 | 板块层面资金 | 各板块主力净流入排行（钱往哪个板块集中） |

**交互**：榜单页多 Tab（涨幅/成交额/涨速/资金流），3~5 秒自动刷新，点击个股跳转详情。

### 功能三：盘中实时查询

**目标**：盘中随时查"某只股票现在怎么样 / 现在最热的是什么"。

| 模块 | 核心内容 | 关键指标 |
|---|---|---|
| 个股实时查询 | 搜索代码/名称 → 实时快照 | 现价、涨跌幅、今开、最高/最低、成交量额、量比、换手率、内外盘 |
| 个股深度数据 | 分时与盘口 | 分时走势图、五档盘口、成交明细 |
| 实时榜单 | 盘中热榜 | 涨速榜、涨幅榜、资金流榜持续刷新 |
| 自选股 | 个人盯盘列表 | 收藏个股，盘中统一刷新展示（前端本地存储） |
| 阈值提醒（P2） | 异动通知 | 价格/涨跌幅/涨速达到阈值时浏览器提示 |

**交互**：顶部搜索框随时查股；自选股侧栏/页面常驻刷新。

---

## 三、需求拆分

### 3.1 模块划分

```
┌─────────────────────────────────────────────┐
│  前端（Web 页面，3~5s 轮询）                    │
│  盘面总览 │ 板块分析 │ 榜单(涨速/资金) │ 个股 │ 自选 │
└──────────────────┬──────────────────────────┘
                   │ REST API（FastAPI）
┌──────────────────┴──────────────────────────┐
│  服务层：路由、交易时段判断、轮询调度、缓存        │
├─────────────────────────────────────────────┤
│  分析层：大盘聚合 / 板块排行 / 涨速 / 资金流聚合   │
├─────────────────────────────────────────────┤
│  数据层：统一数据模型 + 数据源适配器 + 重试/限频   │
└──────────────────┬──────────────────────────┘
                   │ HTTP
        东方财富公开行情接口（push2.eastmoney.com）
```

### 3.2 优先级清单

**P0 — 核心闭环（先做这些就能用）**
1. 数据层：东方财富行情客户端（指数快照、个股快照、板块列表/成分、资金流、涨速）
2. 数据层：统一数据模型（Pydantic dataclass）+ 请求重试
3. 分析层：大盘概况聚合（指数 + 涨跌家数 + 成交额 + 涨停数）
4. 分析层：板块排行（行业/概念）+ 涨速榜 + 资金流榜 + 热门股多维度榜单
5. 服务层：FastAPI 接口 + 盘中调度（仅交易时段轮询）
6. 前端：盘面总览页、板块列表页、榜单页、个股查询页 + 自动刷新

**P1 — 增强体验**
7. 板块详情页（成分股 + 板块资金流）
8. 个股深度：分时图、五档盘口、成交明细
9. 自选股管理（localStorage 持久化）
10. 交易日历与休市判断（节假日不空转请求）

**P2 — 可选进阶**
11. 阈值提醒（价格/涨跌幅/涨速）+ 浏览器通知
12. WebSocket 推送替代轮询
13. 历史数据与复盘：5 日资金流趋势、K 线图
14. Docker 部署 / 一键启动脚本

### 3.3 关键设计决策

| 决策点 | 结论 | 理由 |
|---|---|---|
| 数据源 | 自写薄客户端直连东方财富 push2 接口 | 轮询频率可控、字段清晰、只依赖 httpx；akshare 作为备选对照 |
| 涨速 | 优先直接用东财接口涨速字段（5 分钟涨速），自定义 1/3 分钟涨速列为 P2 | 降低自维护价格窗口的复杂度 |
| 北向资金 | **不纳入实时指标** | 2024 年 8 月起交易所已停止盘中披露北向资金，公开接口拿不到实时值；用涨跌家数/成交额/涨停数代替情绪指标 |
| 刷新策略 | 前端 3~5 秒轮询；后端带短缓存（1~2 秒）防重复请求 | 简单可靠，先不做 WebSocket |
| 交易时段 | 后端统一判断（9:30–11:30、13:00–15:00 + 节假日），非盘中降频或停轮询 | 避免浪费请求、数据无意义 |

---

## 四、技术方案

### 4.1 技术栈

- **后端**：Python 3.11+，FastAPI + uvicorn，httpx（异步 HTTP），Pydantic（模型/校验）
- **前端**：Vue 3 + Vite + ECharts（图表）+ fetch 轮询；轻量 UI 用朴素 CSS 或 Element Plus（按需）
- **工程**：venv + requirements.txt、单测（pytest，重点测数据解析）、启动脚本 `start.sh`
- **目录结构**

```
niulai/
├── backend/
│   ├── app.py              # FastAPI 入口
│   ├── config.py           # 配置（刷新频率、数据源、时段）
│   ├── datasource/
│   │   ├── eastmoney.py    # 东方财富客户端（各接口封装）
│   │   └── models.py       # 统一数据模型（Pydantic）
│   ├── analyzer/
│   │   ├── market.py       # 大盘概况聚合
│   │   ├── sector.py       # 板块排行/详情
│   │   ├── rank.py         # 热门股/涨速/资金流榜单
│   │   └── schedule.py     # 交易时段判断
│   └── api/
│       └── routes.py       # REST 路由
├── frontend/               # Vue3 + Vite + ECharts
│   ├── src/pages/          # 总览 / 板块 / 榜单 / 个股 / 自选
│   └── src/api.js          # 调后端接口
├── scripts/verify.py       # CLI 验证脚本（先于前端打通数据）
└── PLAN.md
```

### 4.2 核心 API 草案

```
GET /api/market/overview      # 大盘概况：指数、涨跌家数、成交额、涨停数
GET /api/sectors?type=industry|concept   # 板块排行
GET /api/sectors/{code}       # 板块详情：成分股 + 板块资金流
GET /api/rank/hot?by=amount|pct|turnover|volume_ratio
GET /api/rank/zhangsu         # 涨速榜
GET /api/rank/moneyflow       # 个股主力净流入榜
GET /api/sectors/moneyflow    # 板块资金流榜
GET /api/stocks/{code}        # 个股实时快照
GET /api/stocks/{code}/ticks  # 分时/盘口（P1）
```

---

## 五、里程碑计划

| 里程碑 | 内容 | 验收标准 | 预计 |
|---|---|---|---|
| **M1 数据打通** | 项目骨架、东财客户端、统一模型、`scripts/verify.py` | CLI 能打印出：指数行情、板块排行、涨速榜、资金流榜（真实数据） | 0.5~1 天 |
| **M2 分析层** | 大盘聚合、板块详情、榜单排序、交易时段判断 | 所有分析函数有单测；CLI 输出与东财网页数据一致 | 0.5~1 天 |
| **M3 后端 API** | FastAPI 路由 + 短缓存 + 盘中调度 | curl 所有接口返回正确 JSON | 0.5 天 |
| **M4 前端** | 总览/板块/榜单/个股 4 页面 + 3~5s 轮询 + ECharts 分时图 | 浏览器可完整走通"看盘 → 查板块 → 看榜单 → 查个股" | 1~2 天 |
| **M5 打磨** | 自选股、异常提示、README、启动脚本、P1 项 | 非盘中页面不报错、断网有提示；文档齐全 | 0.5~1 天 |

**总计约 3~5 天**（业余时间）。M1 是最大风险点（免费接口字段解析），必须最先做技术验证。

---

## 六、风险与注意事项

1. **免费接口不稳定/字段变化**：东财接口字段是数字索引，需维护映射表；客户端做重试 + 超时 + 降级提示。
2. **北向资金已停盘中披露**：不要设计依赖实时北向资金的功能。
3. **交易时段外数据无意义**：休市/节假日由后端判断，避免空转请求。
4. **请求频率**：轮询控制在 3~5 秒，后端 1~2 秒缓存去重，避免被数据源限流。
5. **合规**：自用分析工具，不涉及交易下单；数据仅个人使用。

---

## 七、下一步行动

1. ✅ 确认需求与决策（本次完成）
2. ⬜ **M1 开始**：初始化项目骨架 + 写东方财富客户端 + verify 脚本，先验证数据链路是否真实可用
3. ⬜ 数据打通后进入 M2~M5 逐里程碑推进
